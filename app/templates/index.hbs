<div class="row">
  <div class="col-md-6 col-md-offset-3 well">
    {{#each model.posts as |post|}}
    {{post-tile post=post update="update"}}
    {{/each}}
  </div>
</div>
  {{#link-to 'post-form'}}new post{{/link-to}}

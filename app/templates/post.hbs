<div class="container">
  <img src={{model.image}} alt={{model.title}}>

  <h2>{{model.title}}</h2>
  <h4>{{model.date}}</h4>

  <p>{{model.body}}</p>
  {{update-post post=model update="update"}}
  {{delete-post post=model destroyPost="destroyPost"}}
</div>
